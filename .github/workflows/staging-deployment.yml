name: Staging build and deploy

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the main branch
on:
  push:
    branches: [staging]
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'info'

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build_and_push:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    container: node:14

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - name: Build and Publish to Github Packages Registry
        uses: elgohr/Publish-Docker-Github-Action@master
        env:
          BACKEND_URL: ${{ secrets.FRONTEND_STAGING_PUBLIC_BACKEND_STAGING_URL }}
        with:
          name: dedogmadao/dt-frontend/frontend_staging
          registry: ghcr.io
          username: ${{ secrets.STAGING_GITHUB_USERNAME }}
          password: ${{ secrets.GITHUB_TOKEN }}
          dockerfile: Dockerfile
          buildargs: BACKEND_URL
          tags: latest

  deploy:
    needs: build_and_push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy package to Server
        uses: fifsky/ssh-action@master
        with:
          command: |
            docker login ghcr.io -u dedogmadao -p $GITHUB_TOKEN
            docker pull ghcr.io/dedogmadao/dt-frontend/frontend_staging:latest
<<<<<<< HEAD
            docker stop frontend
            docker system prune -f
            docker network create ddd_network || true
            docker run --name frontend -dit --network=ddd_network ghcr.io/dedogmadao/dt-frontend/frontend_staging:latest
=======
            docker stop front-staging
            docker system prune -f
            docker run --name front-staging -dit -p 80:3000 ghcr.io/dedogmadao/dt-frontend/frontend_staging:latest
>>>>>>> d0b16ffdd53004cb4d763d6aba07a3ccf871d12a
          host: ${{ secrets.FRONTEND_STAGING_DEPLOY_HOST }}
          user: ${{ secrets.FRONTEND_STAGING_DEPLOY_USER }}
          key: ${{ secrets.STAGING_GITHUB_SSH_KEY}}